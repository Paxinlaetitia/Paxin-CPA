/* Recurso privado: entregue somente após autorização owner. */
.admin-gate[hidden],.admin-main[hidden],.admin-page-panel[hidden],.admin-form [hidden] { display: none !important; }
.admin-main { min-height: 100vh; padding-bottom: 24px; background: #070707; }
.admin-topbar { min-height: 52px; display: flex; align-items: center; border-bottom: 1px solid var(--line); background: #070707; }
.admin-topbar > .container { width: min(calc(100% - 40px),1320px); max-width: none; display: flex; align-items: center; justify-content: space-between; gap: 16px; }
.admin-topbar > .container > div { display: flex; align-items: center; gap: 12px; }
.admin-topbar > .container > div > span { color: var(--muted); font-size: 8px; font-weight: 800; letter-spacing: 0.04em; }
.admin-brand { display: inline-flex; align-items: center; gap: 8px; color: var(--text); }
.admin-brand > span { width: 28px; height: 28px; display: grid; place-items: center; border: 1px solid rgba(240,165,0,.28); border-radius: 7px; background: var(--accent-soft); }
.admin-brand img { width: 18px; filter: invert(69%) sepia(98%) saturate(1593%) hue-rotate(358deg) brightness(99%) contrast(105%); }
.admin-brand b { font-size: 13px; }
.admin-brand small { padding-left: 8px; border-left: 1px solid var(--line-strong); color: var(--muted); font-size: 9px; }
.admin-shell { width: min(calc(100% - 40px),1320px); max-width: none; padding-top: 18px; }
.admin-head { padding-bottom: 14px; }
.admin-head > div > span,.admin-dialog header span,.admin-overview-grid article > span { color: var(--accent); font-size: 8px; font-weight: 800; letter-spacing: 0.05em; }
.admin-head h1 { margin: 4px 0 2px; font-size: clamp(22px,2.8vw,30px); line-height: 1.1; text-wrap: balance; }
.admin-head p { max-width: 650px; margin: 0; color: var(--text-2); font-size: 10px; line-height: 1.45; }
.admin-tabs { display: flex; gap: 2px; padding: 3px; overflow-x: auto; border: 1px solid var(--line); border-radius: 10px; background: #0a0a0a; scrollbar-width: none; }
.admin-tabs::-webkit-scrollbar { display: none; }
.admin-tabs button { min-height: 32px; display: inline-flex; flex: 0 0 auto; align-items: center; gap: 6px; padding: 0 10px; border: 0; border-radius: 7px; background: transparent; color: var(--muted); font: inherit; font-size: 10px; font-weight: 700; cursor: pointer; transition: all 0.15s ease; }
.admin-tabs button:hover { color: var(--text); }
.admin-tabs button.is-active { background: var(--accent-soft); color: var(--accent-bright); }
.admin-tabs svg { width: 13px; height: 13px; }
.admin-pages { min-height: auto; padding-top: 14px; }
.admin-page-panel.is-active { animation: admin-panel-enter 180ms ease-out both; }
.admin-panel-toolbar { min-height: 34px; display: flex; align-items: center; justify-content: flex-end; gap: 8px; margin-bottom: 10px; }
.admin-panel-toolbar .button,
.admin-panel-toolbar button { height: 34px; min-height: 34px; padding: 0 12px; font-size: 10px; font-weight: 700; border-radius: 8px; display: inline-flex; align-items: center; justify-content: center; gap: 6px; white-space: nowrap; line-height: 1; }
.admin-metrics { display: grid; grid-template-columns: repeat(4,minmax(0,1fr)); gap: 8px; }
.admin-metrics article,.admin-card,.admin-table-card,.admin-overview-grid article { border: 1px solid var(--line); border-radius: 12px; background: #0a0a0a; }
.admin-metrics article { min-height: 80px; display: flex; flex-direction: column; padding: 12px 14px; }
.admin-metrics span { color: var(--accent); font-size: 8px; font-weight: 800; }
.admin-metrics b { margin: auto 0 2px; font-size: 21px; font-variant-numeric: tabular-nums; }
.admin-metrics small { color: var(--muted); font-size: 8px; }
.admin-overview-grid { display: grid; grid-template-columns: repeat(3,minmax(0,1fr)); gap: 8px; margin-top: 8px; }
.admin-overview-grid article { min-height: 120px; display: flex; flex-direction: column; padding: 14px 16px; }
.admin-overview-grid h3 { margin: 6px 0 3px; font-size: 14px; }
.admin-overview-grid p { margin: 0; color: var(--text-2); font-size: 9px; line-height: 1.4; }
.admin-overview-grid button { display: inline-flex; align-items: center; gap: 6px; width: max-content; margin-top: auto; padding: 0; border: 0; background: transparent; color: var(--accent-bright); font: inherit; font-size: 9px; font-weight: 700; cursor: pointer; }
.admin-overview-grid button svg { width: 11px; height: 11px; }
.admin-table-card { overflow: hidden; box-shadow: 0 14px 40px rgba(0,0,0,.22); }
.admin-table-wrap { overflow: auto; max-height: calc(100vh - 230px); }
.admin-table-card table { width: 100%; border-collapse: collapse; font-size: 10px; }
.admin-table-card th,.admin-table-card td { padding: 9px 14px; border-bottom: 1px solid var(--line); text-align: left; white-space: nowrap; }
.admin-table-card th { color: var(--muted); font-size: 8px; letter-spacing: .04em; background: #080808; position: sticky; top: 0; z-index: 1; }
.admin-table-card td { color: var(--text-2); font-variant-numeric: tabular-nums; }
.admin-table-card td:first-child { color: var(--text); font-weight: 700; }
.admin-row-actions { display: flex; gap: 5px; }
.admin-cell-note { display: block; margin-top: 2px; color: var(--muted); font-size: 8px; font-weight: 500; }
.admin-row-actions button { min-height: 26px; padding: 0 8px; border: 1px solid var(--line-strong); border-radius: 6px; background: #0e0e0e; color: var(--text-2); font: inherit; font-size: 8px; font-weight: 600; cursor: pointer; transition: all 0.15s ease; }
.admin-row-actions button:hover { border-color: rgba(255,255,255,0.25); color: #fff; }
.admin-row-actions button.admin-btn-approve { border-color: rgba(85,182,133,.38); color: #55b685; background: rgba(85,182,133,.08); }
.admin-row-actions button.admin-btn-approve:hover { background: rgba(85,182,133,.2); color: #78d4a6; border-color: rgba(85,182,133,.65); }
.admin-row-actions button.danger { border-color: rgba(235,91,91,.22); color: #f08a8a; }
.admin-row-actions button.danger:hover { background: rgba(235,91,91,.12); color: #ff9d9d; }
.admin-status { padding: 3px 6px; border-radius: 999px; background: rgba(255,255,255,.05); color: var(--muted); font-size: 8px; font-weight: 800; text-transform: uppercase; }
.admin-status.active,.admin-status.paid { background: rgba(85,182,133,.1); color: var(--green); }
.admin-status.paused,.admin-status.revoked,.admin-status.cancelled,.admin-status.expired,.admin-status.closed { background: rgba(235,91,91,.08); color: #f08a8a; }
.admin-status.open,.admin-status.in_progress { background: var(--accent-soft); color: var(--accent-bright); }
.admin-status.resolved { background: rgba(85,182,133,.1); color: var(--green); }
.admin-search { min-width: 260px; height: 34px; min-height: 34px; display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 8px; padding-left: 10px; border: 1px solid var(--line-strong); border-radius: 8px; background: #080808; }
.admin-search svg { color: var(--muted); width: 13px; height: 13px; }
.admin-search input { min-width: 0; height: 100%; border: 0; outline: 0; background: transparent; color: var(--text); font-size: 10px; }
.admin-search button { height: 26px; min-height: 26px; margin-right: 4px; padding: 0 10px; border: 1px solid rgba(240,165,0,.22); border-radius: 6px; background: var(--accent-soft); color: var(--accent-bright); font: inherit; font-size: 8px; font-weight: 700; cursor: pointer; }
.admin-privacy-note { max-width: 520px; margin: 0 0 0 auto; color: var(--muted); font-size: 9px; line-height: 1.4; text-align: right; }
.admin-risk-score { display: inline-grid; min-width: 34px; height: 24px; place-items: center; border: 1px solid var(--line-strong); border-radius: 6px; background: #080808; font-weight: 800; font-size: 9px; }
.admin-risk-score.watch { border-color: rgba(240,165,0,.35); color: var(--accent); }
.admin-risk-score.restricted { border-color: rgba(255,88,88,.42); color: #ff7777; }
.admin-access-shell { display: flex; justify-content: center; width: 100%; padding: 4px 0 16px; }
.admin-access-card.horizontal { width: 100%; max-width: 820px; padding: 22px 26px; border: 1px solid rgba(255,255,255,0.08); border-radius: 14px; background: linear-gradient(180deg, #101012 0%, #09090a 100%); box-shadow: 0 16px 48px rgba(0,0,0,0.45); }
.admin-access-horizontal-grid { display: grid; grid-template-columns: 1fr 1.15fr; gap: 24px; align-items: start; }
.admin-access-col { display: flex; flex-direction: column; gap: 10px; }
.admin-card-header { margin-bottom: 8px; }
.admin-card-badge { display: inline-flex; align-items: center; gap: 5px; padding: 3px 8px; border-radius: 20px; background: rgba(240,165,0,0.1); border: 1px solid rgba(240,165,0,0.22); color: var(--accent); font-size: 8px; font-weight: 800; letter-spacing: 0.05em; text-transform: uppercase; margin-bottom: 6px; }
.admin-card-header h2 { margin: 0 0 2px; font-size: 18px; font-weight: 700; color: #fff; letter-spacing: -0.02em; }
.admin-card-header p { margin: 0; color: var(--text-2); font-size: 10px; line-height: 1.4; }
.admin-form-group { display: flex; flex-direction: column; gap: 4px; }
.admin-form-group label { color: var(--text-2); font-size: 9px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.04em; }
.admin-input-wrap input, .admin-input-wrap select { width: 100%; height: 36px; padding: 0 10px; border: 1px solid var(--line-strong); border-radius: 8px; background: #070708; color: var(--text); font: inherit; font-size: 11px; }
.admin-input-wrap input:focus, .admin-input-wrap select:focus { border-color: var(--accent); box-shadow: 0 0 0 2px rgba(240,165,0,0.12); outline: none; }
.admin-duration-box { display: flex; flex-direction: column; gap: 8px; padding: 10px 12px; border: 1px solid rgba(255,255,255,0.06); border-radius: 10px; background: rgba(255,255,255,0.02); }
.admin-duration-header { display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 4px; }
.admin-duration-header span { color: var(--accent); font-size: 8px; font-weight: 800; letter-spacing: 0.05em; text-transform: uppercase; }
.admin-duration-header small { color: var(--muted); font-size: 8px; }
.admin-duration-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
.admin-time-unit { display: flex; flex-direction: column; gap: 3px; }
.admin-time-unit label { color: var(--muted); font-size: 8px; font-weight: 600; text-align: center; }
.admin-time-unit input { width: 100%; height: 32px; text-align: center; border: 1px solid var(--line-strong); border-radius: 6px; background: #060607; color: #fff; font: inherit; font-size: 11px; font-weight: 700; }
.admin-time-unit input:focus { border-color: var(--accent); box-shadow: 0 0 0 2px rgba(240,165,0,0.12); outline: none; }
.admin-duration-presets { display: flex; flex-wrap: wrap; gap: 5px; }
.admin-duration-presets button { height: 24px; padding: 0 8px; border: 1px solid rgba(255,255,255,0.08); border-radius: 12px; background: rgba(255,255,255,0.04); color: var(--text-2); font: inherit; font-size: 9px; font-weight: 600; cursor: pointer; transition: all 0.15s ease; }
.admin-duration-presets button:hover { border-color: rgba(240,165,0,0.5); background: rgba(240,165,0,0.12); color: var(--accent-bright); }
.admin-info-banner { display: flex; align-items: flex-start; gap: 8px; padding: 8px 10px; border: 1px solid rgba(240,165,0,0.14); border-radius: 8px; background: rgba(240,165,0,0.04); color: var(--text-2); font-size: 9px; line-height: 1.4; }
.admin-info-banner svg { flex-shrink: 0; color: var(--accent); margin-top: 1px; }
.admin-submit-btn { display: inline-flex; align-items: center; justify-content: center; gap: 6px; width: 100%; height: 38px; border: 0; border-radius: 8px; background: linear-gradient(135deg, #f5b014 0%, #e09200 100%); color: #000; font: inherit; font-size: 11px; font-weight: 800; cursor: pointer; transition: all 0.15s ease; box-shadow: 0 4px 16px rgba(240,165,0,0.2); margin-top: 2px; }
.admin-submit-btn:hover { filter: brightness(1.08); transform: translateY(-1px); box-shadow: 0 6px 20px rgba(240,165,0,0.3); }
.admin-form-card { max-width: 900px; padding: 20px; }
.admin-form { display: grid; grid-template-columns: repeat(2,minmax(0,1fr)); gap: 10px; }
.admin-form header { grid-column: 1 / -1; margin-bottom: 2px; }
.admin-form h2 { margin: 2px 0 0; font-size: 20px; }
.admin-form label { display: grid; gap: 4px; color: var(--text-2); font-size: 8px; font-weight: 700; }
.admin-form input,.admin-form select,.admin-form textarea { width: 100%; min-height: 36px; padding: 0 10px; border: 1px solid var(--line-strong); border-radius: 8px; outline: 0; background: #060606; color: var(--text); font: inherit; font-size: 10px; }
.admin-form textarea { min-height: 64px; padding: 8px; resize: vertical; }
.admin-form input:focus-visible,.admin-form select:focus-visible,.admin-form textarea:focus-visible { border-color: rgba(240,165,0,.6); box-shadow: 0 0 0 2px rgba(240,165,0,.08); }
.admin-form .admin-full,.admin-form .admin-check,.admin-form .admin-note,.admin-form > button { grid-column: 1 / -1; }
.admin-form .admin-check { display: flex; align-items: center; gap: 8px; color: var(--muted); }
.admin-form .admin-check input { width: auto; min-height: auto; accent-color: var(--accent); }
.admin-form .admin-note { margin: 0; color: var(--muted); font-size: 8px; line-height:1.45; }
.admin-form > button { justify-content: center; min-height: 38px; }
.admin-duration-fields { grid-column:1/-1; display:grid; grid-template-columns:repeat(3,minmax(0,1fr)); gap:8px; margin:2px 0 0; padding:12px; border:1px solid var(--line); border-radius:10px; }
.admin-duration-fields legend { padding:0 6px; color:var(--text-2); font-size:9px; font-weight:800; text-transform:uppercase; letter-spacing:.08em; }
.admin-dialog { width: min(580px,calc(100% - 32px)); padding: 0; border: 1px solid var(--line-strong); border-radius: 14px; background: #0a0a0a; color: var(--text); box-shadow: 0 24px 80px rgba(0,0,0,.72); }
.admin-dialog::backdrop { background: rgba(0,0,0,.8); backdrop-filter: blur(4px); }
.admin-dialog .admin-form { position: relative; padding: 22px; }
.admin-customer-modal { width: 100%; padding: 22px 24px; display: flex; flex-direction: column; gap: 16px; position: relative; box-sizing: border-box; }
.admin-customer-modal-header { display: flex; justify-content: space-between; align-items: flex-start; gap: 12px; }
.admin-customer-modal-header h2 { margin: 6px 0 2px; font-size: 18px; font-weight: 700; color: #fff; letter-spacing: -0.02em; word-break: break-all; }
.admin-modal-close { width: 28px; height: 28px; min-height: 28px; padding: 0; display: inline-flex; align-items: center; justify-content: center; border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; background: rgba(255,255,255,0.04); color: var(--muted); cursor: pointer; transition: all 0.15s ease; flex-shrink: 0; }
.admin-modal-close:hover { border-color: rgba(255,255,255,0.25); background: rgba(255,255,255,0.1); color: #fff; }
.admin-customer-stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; }
.admin-customer-stat-card { display: flex; align-items: center; gap: 10px; padding: 10px 12px; border: 1px solid rgba(255,255,255,0.06); border-radius: 10px; background: #0c0c0e; box-sizing: border-box; }
.admin-stat-icon { width: 30px; height: 30px; border-radius: 8px; background: rgba(255,255,255,0.05); display: grid; place-items: center; color: var(--muted); flex-shrink: 0; }
.admin-stat-icon.accent { background: rgba(240,165,0,0.1); color: var(--accent); }
.admin-stat-info { display: flex; flex-direction: column; gap: 1px; min-width: 0; }
.admin-stat-label { color: var(--muted); font-size: 8px; font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase; }
.admin-stat-val { color: #fff; font-size: 11px; font-weight: 700; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.admin-customer-actions-section { display: flex; flex-direction: column; gap: 8px; margin-top: 2px; }
.admin-actions-title { color: var(--muted); font-size: 8px; font-weight: 800; letter-spacing: 0.06em; text-transform: uppercase; }
.admin-actions-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; }
.admin-action-btn { display: flex; align-items: center; gap: 10px; padding: 10px 12px; border: 1px solid rgba(255,255,255,0.08); border-radius: 10px; background: #0e0e11; color: var(--text); font: inherit; text-align: left; cursor: pointer; transition: all 0.15s ease; box-sizing: border-box; }
.admin-action-btn:hover { border-color: rgba(255,255,255,0.22); background: #15151a; transform: translateY(-1px); }
.admin-action-btn svg { flex-shrink: 0; color: var(--muted); }
.admin-action-btn:hover svg { color: #fff; }
.admin-action-btn-text { display: flex; flex-direction: column; gap: 1px; }
.admin-action-btn-text strong { font-size: 10px; font-weight: 700; color: #fff; line-height: 1.2; }
.admin-action-btn-text small { font-size: 8px; color: var(--muted); line-height: 1.2; }
.admin-action-btn.primary { border-color: rgba(240,165,0,0.3); background: linear-gradient(135deg, rgba(240,165,0,0.14) 0%, rgba(240,165,0,0.04) 100%); }
.admin-action-btn.primary:hover { border-color: rgba(240,165,0,0.6); background: linear-gradient(135deg, rgba(240,165,0,0.22) 0%, rgba(240,165,0,0.08) 100%); }
.admin-action-btn.primary svg { color: var(--accent); }
.admin-action-btn.primary strong { color: var(--accent-bright); }
.admin-action-btn.warning { border-color: rgba(235,91,91,0.18); background: rgba(235,91,91,0.04); }
.admin-action-btn.warning:hover { border-color: rgba(235,91,91,0.45); background: rgba(235,91,91,0.1); }
.admin-action-btn.warning svg { color: #f08a8a; }
.admin-action-btn.warning strong { color: #ff9d9d; }
.admin-ban-bar { display: flex; align-items: center; justify-content: space-between; gap: 12px; padding: 10px 14px; border: 1px solid rgba(235,91,91,0.2); border-radius: 10px; background: rgba(235,91,91,0.05); margin-top: 4px; box-sizing: border-box; }
.admin-ban-info { display: flex; align-items: center; gap: 10px; }
.admin-ban-info svg { color: #f08a8a; flex-shrink: 0; }
.admin-ban-info div { display: flex; flex-direction: column; gap: 1px; }
.admin-ban-info strong { font-size: 10px; font-weight: 700; color: #ff9d9d; }
.admin-ban-info small { font-size: 8px; color: var(--muted); }
.admin-ban-btn { height: 28px; min-height: 28px; padding: 0 12px; border: 1px solid rgba(235,91,91,0.3); border-radius: 6px; background: rgba(235,91,91,0.15); color: #ff9d9d; font: inherit; font-size: 9px; font-weight: 700; cursor: pointer; transition: all 0.15s ease; white-space: nowrap; }
.admin-ban-btn:hover { background: rgba(235,91,91,0.3); border-color: rgba(235,91,91,0.6); color: #fff; }
.admin-ban-btn.is-banned { background: rgba(85,182,133,0.15); border-color: rgba(85,182,133,0.35); color: #55b685; }
.admin-ban-btn.is-banned:hover { background: rgba(85,182,133,0.25); border-color: rgba(85,182,133,0.6); color: #78d4a6; }
.admin-empty { min-height: 90px; display: grid; place-items: center; padding: 16px; border: 1px dashed var(--line-strong); border-radius: 10px; color: var(--muted); font-size: 9px; text-align: center; }
.admin-ticket-list { display: grid; gap: 6px; }
.admin-ticket-list > article { min-height: 68px; display: grid; grid-template-columns: minmax(0,1fr) auto auto; align-items: center; gap: 14px; padding: 12px 14px; border: 1px solid var(--line); border-radius: 10px; background: #0a0a0a; }
.admin-ticket-list article > div { min-width: 0; }
.admin-ticket-list article > div > span { color: var(--accent); font-size: 8px; font-weight: 800; text-transform: uppercase; }
.admin-ticket-list h3 { overflow: hidden; margin: 3px 0 2px; font-size: 12px; text-overflow: ellipsis; white-space: nowrap; }
.admin-ticket-list p { margin: 0; color: var(--muted); font-size: 8px; }
.admin-ticket-list button { min-height: 28px; padding: 0 9px; border: 1px solid var(--line-strong); border-radius: 6px; background: #0d0d0d; color: var(--text-2); font: inherit; font-size: 8px; cursor: pointer; }
.admin-ticket-dialog { width: min(680px,calc(100% - 32px)); }
.admin-ticket-dialog-content { position: relative; display: grid; gap: 12px; padding: 22px; }
.admin-ticket-dialog header > span { color: var(--accent); font-size: 8px; font-weight: 800; }
.admin-ticket-dialog h2 { margin: 3px 0 2px; font-size: 20px; }
.admin-ticket-dialog header p { margin: 0; color: var(--muted); font-size: 9px; }
.admin-support-thread { max-height: 320px; display: grid; gap: 6px; overflow-y: auto; padding: 10px; border: 1px solid var(--line); border-radius: 10px; background: #070707; }
.admin-support-thread article { max-width: 80%; padding: 8px 10px; border: 1px solid var(--line); border-radius: 8px; background: #0c0c0c; }
.admin-support-thread article.is-owner { margin-left: auto; border-color: rgba(240,165,0,.22); background: var(--accent-soft); }
.admin-support-thread b { font-size: 8px; }
.admin-support-thread p { margin: 3px 0; color: var(--text-2); font-size: 9px; line-height: 1.45; white-space: pre-wrap; }
.admin-support-thread small { color: var(--muted); font-size: 7px; }
#admin-ticket-reply-form { display: grid; gap: 8px; }
#admin-ticket-reply-form label { display: grid; gap: 4px; color: var(--text-2); font-size: 8px; font-weight: 700; }
#admin-ticket-reply-form textarea,#admin-ticket-reply-form select { width: 100%; min-height: 36px; padding: 8px 10px; border: 1px solid var(--line-strong); border-radius: 8px; outline: 0; background: #060606; color: var(--text); font: inherit; font-size: 9px; }
#admin-ticket-reply-form textarea { min-height: 72px; resize: vertical; }
#admin-ticket-reply-form > div { display: grid; grid-template-columns: minmax(140px,.45fr) auto; justify-content: end; gap: 6px; }
.dialog-close { position: absolute; top: 14px; right: 14px; width: 28px; height: 28px; display: grid; place-items: center; border: 1px solid var(--line); border-radius: 6px; background: transparent; color: var(--muted); cursor: pointer; }
.admin-gate { min-height: 100vh; display: grid; place-items: center; padding: 24px; background: #070707; }
.admin-gate > div { width: min(440px,100%); padding: 24px; border: 1px solid var(--line-strong); border-radius: 14px; background: #0a0a0a; }
.admin-gate span { color: var(--accent); font-size: 8px; font-weight: 800; }
.admin-gate h1 { margin: 8px 0 6px; font-size: 26px; }
.admin-gate p { margin: 0 0 18px; color: var(--text-2); font-size: 11px; line-height: 1.5; }
@keyframes admin-panel-enter { from { opacity: 0; transform: translateY(5px); } to { opacity: 1; transform: translateY(0); } }

@media (max-width: 900px) {
  .admin-metrics { grid-template-columns: repeat(2,minmax(0,1fr)); }
  .admin-overview-grid { grid-template-columns: 1fr; }
  .admin-search { width: 100%; min-width: 0; }
}
@media (max-width: 620px) {
  .admin-topbar > .container,.admin-shell { width: min(calc(100% - 24px),1320px); }
  .admin-topbar > .container > div > span,.admin-brand small { display: none; }
  .admin-shell { padding-top: 14px; }
  .admin-head h1 { font-size: 24px; }
  .admin-metrics,.admin-overview-grid,.admin-form { grid-template-columns: 1fr; }
  .admin-form label,.admin-form header,.admin-form .admin-full,.admin-form .admin-check,.admin-form .admin-note,.admin-form > button { grid-column: auto; }
  .admin-duration-fields { grid-column:auto; grid-template-columns:1fr; }
  .admin-duration-presets { grid-column:auto; }
  .admin-panel-toolbar .button { width: 100%; justify-content: center; }
  .admin-gate { padding: 14px; }
  .admin-gate > div { padding: 18px; }
  .admin-ticket-list > article { grid-template-columns: 1fr auto; }
  .admin-ticket-list article > button { grid-column: 1 / -1; }
  #admin-ticket-reply-form > div { grid-template-columns: 1fr; }
}
@media (prefers-reduced-motion: reduce) { .admin-page-panel.is-active { animation: none; } }
